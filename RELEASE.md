# Releases

The completed `v0.2.0` checklist has been replaced by the reusable release
procedure in [`docs/releasing.rst`](docs/releasing.rst).

Use that procedure for every release. Published versions and their
changes remain recorded in [`CHANGELOG.md`](CHANGELOG.md).
