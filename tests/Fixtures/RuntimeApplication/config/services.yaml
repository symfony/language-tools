parameters:
    app.fixture_name: Symfony LSP

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'

    app.fixture_controller:
        class: App\Controller\HomeController

    App\Environment\CustomEnvVarProcessor:
        tags: ['container.env_var_processor']
